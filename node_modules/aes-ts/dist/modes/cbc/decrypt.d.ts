import type { BufferInput } from '../../utils';
import { Block, Decryptor } from '../../block';
export declare const decrypt: (decryptor: Block | Decryptor, ciphertext: BufferInput, state: {
    _lastCipherblock: Uint8Array;
}) => Uint8Array;
