import type { BufferInput } from '../../utils';
import { Block, Encryptor, Decryptor } from '../../block';
import { ModeOfOperationCBC } from './base';
export declare class CBCEncryptor extends ModeOfOperationCBC<Encryptor> {
    constructor(key: BufferInput, iv: BufferInput);
    encrypt(plaintext: BufferInput): Uint8Array;
}
export declare class CBCDecryptor extends ModeOfOperationCBC<Decryptor> {
    constructor(key: BufferInput, iv: BufferInput);
    decrypt(ciphertext: BufferInput): Uint8Array;
}
export declare class CBC extends ModeOfOperationCBC<Block> {
    constructor(key: BufferInput, iv: BufferInput);
    encrypt(plaintext: BufferInput): Uint8Array;
    decrypt(ciphertext: BufferInput): Uint8Array;
}
