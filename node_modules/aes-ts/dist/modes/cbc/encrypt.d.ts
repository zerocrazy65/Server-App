import type { BufferInput } from '../../utils';
import { Block, Encryptor } from '../../block';
export declare const encrypt: (encryptor: Block | Encryptor, plaintext: BufferInput, state: {
    _lastCipherblock: Uint8Array;
}) => Uint8Array;
