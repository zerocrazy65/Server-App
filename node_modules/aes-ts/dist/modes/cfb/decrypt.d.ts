import type { BufferInput } from '../../utils';
import { Encryptor } from '../../block';
export declare const decrypt: (encryptor: Encryptor, ciphertext: BufferInput, state: {
    segmentSize: number;
    _shiftRegister: Uint8Array;
}) => Uint8Array;
