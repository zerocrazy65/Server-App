import type { BufferInput } from '../../utils';
import { Block, Encryptor, Decryptor } from '../../block';
import { ModeOfOperationECB } from './base';
export declare class ECBEncryptor extends ModeOfOperationECB<Encryptor> {
    constructor(key: BufferInput);
    encrypt(plaintext: BufferInput): Uint8Array;
}
export declare class ECBDecryptor extends ModeOfOperationECB<Decryptor> {
    constructor(key: BufferInput);
    decrypt(ciphertext: BufferInput): Uint8Array;
}
export declare class ECB extends ModeOfOperationECB<Block> {
    constructor(key: BufferInput);
    encrypt(plaintext: BufferInput): Uint8Array;
    decrypt(ciphertext: BufferInput): Uint8Array;
}
