import type { BufferInput } from '../../utils';
import { Encryptor } from '../../block';
export declare const encrypt: (encryptor: Encryptor, plaintext: BufferInput, state: {
    _lastPrecipher: Uint8Array;
    _lastPrecipherIndex: number;
}) => Uint8Array;
