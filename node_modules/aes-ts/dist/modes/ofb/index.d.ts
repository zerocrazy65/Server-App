import type { BufferInput } from '../../utils';
import { Encryptor } from '../../block';
import { ModeOfOperationOFB } from './base';
export declare class OFBEncryptor extends ModeOfOperationOFB<Encryptor> {
    constructor(key: BufferInput, iv: BufferInput);
    encrypt(plaintext: BufferInput): Uint8Array;
}
export declare class OFBDecryptor extends ModeOfOperationOFB<Encryptor> {
    constructor(key: BufferInput, iv: BufferInput);
    decrypt(ciphertext: BufferInput): Uint8Array;
}
export declare class OFB extends ModeOfOperationOFB<Encryptor> {
    constructor(key: BufferInput, iv: BufferInput);
    encrypt(plaintext: BufferInput): Uint8Array;
    decrypt(ciphertext: BufferInput): Uint8Array;
}
