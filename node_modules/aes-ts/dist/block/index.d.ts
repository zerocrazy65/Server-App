import { Base } from './base';
export declare type Instance = Block | Encryptor | Decryptor;
export declare class Encryptor extends Base {
    encrypt(plaintext: Uint8Array): Uint8Array;
}
export declare class Decryptor extends Base {
    decrypt(ciphertext: Uint8Array): Uint8Array;
}
export declare class Block extends Base {
    encrypt(plaintext: Uint8Array): Uint8Array;
    decrypt(ciphertext: Uint8Array): Uint8Array;
}
