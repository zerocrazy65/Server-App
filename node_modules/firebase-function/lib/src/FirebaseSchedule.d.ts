import * as functions from 'firebase-functions';
/**
 * Firebase schedule with a execute method.
 */
export interface FirebaseSchedule {
    /**
     * Executes the firebase schedule.
     */
    executeFunction(): Promise<void>;
}
export declare namespace FirebaseSchedule {
    function create<FSchedule extends FirebaseSchedule>(schedule: string, createFirebaseSchedule: (context: functions.EventContext<Record<string, string>>) => FSchedule): functions.CloudFunction<unknown>;
}
