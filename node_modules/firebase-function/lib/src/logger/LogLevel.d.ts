export declare class LogLevel {
    private readonly value;
    constructor(value: LogLevel.Value);
    coloredText(text: string, isColored: boolean): string;
}
export declare namespace LogLevel {
    type Value = 'debug' | 'info' | 'notice';
}
