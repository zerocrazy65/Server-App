import { type DatabaseType } from '../DatabaseType';
import { type ILogger } from './ILogger';
export declare class VerboseType {
    private readonly value;
    constructor(value: VerboseType.Value);
    get isVerbose(): boolean;
    get isColored(): boolean;
}
export declare namespace VerboseType {
    type Value = 'none' | 'verbose' | 'colored' | 'coloredVerbose';
    function fromString(value: string, databaseType: DatabaseType, logger: ILogger): VerboseType;
}
