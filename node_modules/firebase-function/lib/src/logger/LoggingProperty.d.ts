import { type LogLevel } from './LogLevel';
import { type VerboseType } from './VerboseType';
export declare class LoggingProperty {
    private readonly functionName;
    private readonly logLevel;
    private readonly indent;
    private readonly details?;
    constructor(functionName: string, logLevel: LogLevel, indent: number, details?: Record<PropertyKey, unknown> | undefined);
    private prefix;
    completeLog(verbose: VerboseType): string;
    private detailString;
}
