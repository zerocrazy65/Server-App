export declare class CombineIterator<T1, T2, R> implements Iterator<R, undefined> {
    private readonly iterator1;
    private readonly iterator2;
    private readonly combineElement;
    constructor(iterator1: Iterator<T1>, iterator2: Iterator<T2>, combineElement: (element1: T1, element2: T2) => R);
    next(): IteratorResult<R, undefined>;
}
