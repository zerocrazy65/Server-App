export declare class PseudoRandom {
    private readonly INITIAL_MASH_N;
    private readonly state;
    constructor(seed: Uint8Array);
    private mash;
    private mashResult;
    private random;
    randomByte(): number;
}
