export declare class RandomBitIterator implements Iterator<0 | 1, undefined> {
    private readonly pseudoRandom;
    private bitIterator;
    constructor(seed: Uint8Array);
    next(): IteratorResult<0 | 1, undefined>;
}
