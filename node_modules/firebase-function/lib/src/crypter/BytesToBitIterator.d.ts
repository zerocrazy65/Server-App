export declare class BytesToBitIterator implements Iterator<0 | 1, undefined> {
    private readonly bytesIterator;
    private currentBitsIterator;
    constructor(bytes: Uint8Array);
    next(): IteratorResult<0 | 1, undefined>;
}
